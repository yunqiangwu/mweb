<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[WuYun Blog]]></title>
  <link href="https://blog.jajabjbj.top/atom.xml" rel="self"/>
  <link href="https://blog.jajabjbj.top/"/>
  <updated>2020-03-06T23:12:40+08:00</updated>
  <id>https://blog.jajabjbj.top/</id>
  <author>
    <name><![CDATA[]]></name>
    
  </author>
  <generator uri="http://www.mweb.im/">MWeb</generator>
  
  <entry>
    <title type="html"><![CDATA[c7n-ui codepen 在线demo 问题修复]]></title>
    <link href="https://blog.jajabjbj.top/15835043241284.html"/>
    <updated>2020-03-06T22:18:44+08:00</updated>
    <id>https://blog.jajabjbj.top/15835043241284.html</id>
    <content type="html"><![CDATA[
<h2 id="toc_0">解决 c7n-ui codepen 在线代码演示 ，通过拿不到组件库。</h2>

<p>当我们打开 <a href="https://choerodon.github.io/choerodon-ui/">https://choerodon.github.io/choerodon-ui/</a> 点击网站 codepen 在线预览，代码无法预览。</p>

<p>codepen 的依赖都是通过添加 <code>&lt;script src=&quot;https://unpkg.com/choerodon-ui@0.8.49/dist/choerodon-ui-with-locales.js&quot;&gt;&lt;/script&gt;</code><br/>
把组件库引入到浏览器环境中。</p>

<p>由于 codepen 环境运行的代码只是经过 babel 简单处理一下， 但是不不支持 import 语法，所以打开 codepen 之后。 import 语句：</p>

<pre><code class="language-text">import { Button } from &#39;choerodon-ui&#39;;
</code></pre>

<p>会被转换成</p>

<pre><code class="language-text">const { Button } = window[&quot;choerodon-ui&quot;];
</code></pre>

<p>所以 codepen 引入的依赖库都是需要挂载到 global 全局对象上面,通过全局对象引用。</p>

<p>类似这样 <code>window.React</code>、<code>window.Vue</code>、<code>window[&quot;choerodon-ui&quot;]</code> 在代码中通过 全局对象。</p>

<p>必须保证 <code>https://unpkg.com/choerodon-ui@0.8.49/dist/choerodon-ui-with-locales.js</code> 中有挂载语句：</p>

<pre><code class="language-text">window[&quot;choerodon-ui&quot;] = ...
</code></pre>

<p>我们之间输出一下 <code>window[&quot;choerodon-ui&quot;]</code> 对象 测试一下:<br/>
<img src="media/15835043241284/15835059729329.jpg" alt="" style="width:722px;"/></p>

<p>可以看到 <code>window[&quot;choerodon-ui&quot;]</code> 值为 undefined</p>

<p>然后我们先来看一下 经过 webpack 编译后输出的 <code>https://unpkg.com/choerodon-ui@0.8.49/dist/choerodon-ui-with-locales.js</code> 内容：</p>

<pre><code class="language-javascript">/*!
 * 
 * choerodon-ui v0.8.49
 *       
 */
(function webpackUniversalModuleDefinition(root, factory) {
    if(typeof exports === &#39;object&#39; &amp;&amp; typeof module === &#39;object&#39;)
        module.exports = factory(require(&quot;mobx&quot;), require(&quot;moment&quot;), require(&quot;react&quot;), require(&quot;react-dom&quot;));
    else if(typeof define === &#39;function&#39; &amp;&amp; define.amd)
        define([&quot;mobx&quot;, &quot;moment&quot;, &quot;react&quot;, &quot;react-dom&quot;], factory);
    else if(typeof exports === &#39;object&#39;)
        exports[&quot;choerodon-ui-with-locales&quot;] = factory(require(&quot;mobx&quot;), require(&quot;moment&quot;), require(&quot;react&quot;), require(&quot;react-dom&quot;));
    else
        root[&quot;choerodon-ui-with-locales&quot;] = factory(root[&quot;mobx&quot;], root[&quot;moment&quot;], root[&quot;React&quot;], root[&quot;ReactDOM&quot;]);
})(window,function(__WEBPACK_EXTERNAL_MODULE_mobx__, __WEBPACK_EXTERNAL_MODULE_moment__, __WEBPACK_EXTERNAL_MODULE_react__, __WEBPACK_EXTERNAL_MODULE_react_dom__) {
    // ...
    // return ...
});

</code></pre>

<p>我们可以看到这里有一个 <code>root[&quot;choerodon-ui-with-locales&quot;]</code> 这里 <code>root</code> 就是全局对象 <code>window</code>, 但是这里挂载的字段不对。应该是<code>choerodon-ui</code>，为什么会这样了。</p>

<p>其实这是一种 webpack 的输出 dist 机制, 当检测到当前是浏览器环境时，会把整个组件库通过 entry name 挂载到全局对象上面。 entry name 就是在 webpack entry 配置来指定的， 比如：</p>

<pre><code class="language-javascript">{
    entry: {
        &#39;abcd&#39;: &#39;./src/abcd/index.js&#39;,
    }
}
</code></pre>

<p>这里最后生成的 dist 中 abcd 就是 entry name, 如果 entry 配置的值是 数组 或者 字符串， 没有指定 entry ， 那么 entry 就会变成 main。</p>

<p>我们看一下 c7n-ui <a href="https://github.com/choerodon/choerodon-ui/blob/master/webpack.config.js#L16-L28">webpack 配置</a>： </p>

<pre><code class="language-javascript">function addLocales(webpackConfig) {
  let packageName = `${pkg.name}-with-locales`;
  let packageNamePro = `${pkg.name}-pro-with-locales`;
  if (webpackConfig.entry[`${pkg.name}.min`]) {
    packageName += &#39;.min&#39;;
  }
  if (webpackConfig.entry[`${pkg.name}-pro.min`]) {
    packageNamePro += &#39;.min&#39;;
  }
  webpackConfig.entry[packageName] = &#39;./index-with-locales.js&#39;;
  webpackConfig.entry[packageNamePro] = &#39;./index-pro-with-locales.js&#39;;
  webpackConfig.output.filename = &#39;[name].js&#39;;
}
</code></pre>

<p>这里多输出了两个包含多语言数据的包。但是也修改了 entry name。 所以导致 <code>window[&quot;choerodon-ui&quot;]</code> 变成了 <code>window[&quot;choerodon-ui-with-locales&quot;]</code>,那么如何修改把它修复这个问题呢。</p>

<p>既然是 webpack 的锅， 所以我去 webpack 官网找了一圈， 最终还是没有找到修改 dist 文件中的 entry name 的方法。</p>

<p>最后想了一下, dist 的内容不就是文件内容的文本替换吗。 直接写一个 webpack plugins 就可以解决这个问题 ， 最后通过开发 webpack plugin 修改 webpack 输出 dist 指定生成的  global name </p>

<p>webpack plugin 代码实现:</p>

<pre><code class="language-javascript">
const webpackConfig = {
    plugins:[
    {
      apply: compiler =&gt; {
        const replaceLibraryConfigList = [
          {
            from: /choerodon-ui(-with-locales(\.min)?)/gi,
            to: &#39;choerodon-ui&#39;,
          },
          {
            from: /choerodon-ui-pro(-with-locales(\.min)?)/gi,
            to: &#39;choerodon-ui/pro&#39;,
          },
        ];
        compiler.hooks.compilation.tap(&#39;compilation&#39;, compilation =&gt; {
          const { mainTemplate, chunkTemplate } = compilation;
          // eslint-disable-next-line no-restricted-syntax
          for (const template of [mainTemplate, chunkTemplate]) {
            template.hooks.renderWithEntry.tap(&#39;UmdMainTemplatePlugin&#39;, (source, entry) =&gt; {
              const entryName = entry.name;
              const findReplaceCfg = replaceLibraryConfigList.find(replaceCfg =&gt; {
                return replaceCfg.from.test(entryName);
              });
              if (findReplaceCfg) {
                let newSource = source;
                let startPos = -1;
                // eslint-disable-next-line no-cond-assign
                while (
                  (startPos = newSource
                    .source()
                    .indexOf(entryName, startPos &gt; 0 ? startPos : undefined)) !== -1
                ) {
                  newSource = new ReplaceSource(newSource);
                  newSource.replace(startPos, startPos + entryName.length - 1, findReplaceCfg.to);
                }
                return newSource;
              }
              return source;
            });
          }
        });
      },
    }]
};
</code></pre>

<p><a href="https://github.com/choerodon/choerodon-ui/blob/master/webpack.config.js#L45">源码位置</a></p>

<p>这个插件的原理很简单， 就是通过正则表达式找到  <code>window[&quot;choerodon-ui-with-locales&quot;]</code> 替换成  <code>window[&quot;choerodon-ui&quot;]</code></p>

<p>处理之后生成的 dist：</p>

<ul>
<li><a href="https://unpkg.com/choerodon-ui@0.8.56/dist/choerodon-ui-with-locales.js">https://unpkg.com/choerodon-ui@0.8.56/dist/choerodon-ui-with-locales.js</a></li>
</ul>

<pre><code class="language-javascript">/*!
 * 
 * choerodon-ui v0.8.56
 *       
 */
(function webpackUniversalModuleDefinition(root, factory) {
    if(typeof exports === &#39;object&#39; &amp;&amp; typeof module === &#39;object&#39;)
        module.exports = factory(require(&quot;mobx&quot;), require(&quot;moment&quot;), require(&quot;react&quot;), require(&quot;react-dom&quot;));
    else if(typeof define === &#39;function&#39; &amp;&amp; define.amd)
        define([&quot;mobx&quot;, &quot;moment&quot;, &quot;react&quot;, &quot;react-dom&quot;], factory);
    else if(typeof exports === &#39;object&#39;)
        exports[&quot;choerodon-ui&quot;] = factory(require(&quot;mobx&quot;), require(&quot;moment&quot;), require(&quot;react&quot;), require(&quot;react-dom&quot;));
    else
        root[&quot;choerodon-ui&quot;] = factory(root[&quot;mobx&quot;], root[&quot;moment&quot;], root[&quot;React&quot;], root[&quot;ReactDOM&quot;]);
})(window, function(__WEBPACK_EXTERNAL_MODULE_mobx__, __WEBPACK_EXTERNAL_MODULE_moment__, __WEBPACK_EXTERNAL_MODULE_react__, __WEBPACK_EXTERNAL_MODULE_react_dom__) {});
</code></pre>

<p>到此，问题解决：</p>

<p><img src="media/15835043241284/15835072613146.jpg" alt="" style="width:982px;"/></p>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[前端持续集成（gitlab-ci 、 jenkins）]]></title>
    <link href="https://blog.jajabjbj.top/15820942246314.html"/>
    <updated>2020-02-19T14:37:04+08:00</updated>
    <id>https://blog.jajabjbj.top/15820942246314.html</id>
    <content type="html"><![CDATA[
<h2 id="toc_0">jenkins + nginx 实现持续集成</h2>

<h3 id="toc_1">搭建jenkins</h3>

<p>jenkins 是一个开源持续集成工具，安装非常简单，为了管理方便，这里使用 docker 进行安装。</p>

<h4 id="toc_2">安装 docker</h4>

<p>centos 安装 docker </p>

<pre><code class="language-bash">sudo yum install docker
</code></pre>

<p>启动 docker 服务</p>

<pre><code class="language-bash">sudo systemctl start docker
</code></pre>

<h4 id="toc_3">安装 jenkins</h4>

<p>创建 jenkins 数据持久化目录</p>

<pre><code class="language-bash">sudo mkdir -p /data
sudo chmod 777 /data
mkdir -p /data/jenkins_home
</code></pre>

<p>通过docker容器安装jenkins</p>

<pre><code class="language-bash">docker run \
  -u root \
  --rm \
  --name jenkins-server \
  -d \
  -p 8080:8080 \
  -p 50000:50000 \
  -v /data/jenkins_home:/var/jenkins_home \
  -v /var/run/docker.sock:/var/run/docker.sock \
  registry.cn-hangzhou.aliyuncs.com/hzero-cli/jenkins
</code></pre>

<blockquote>
<p>关于 docker 命令的学习可以看这里 👉 <a href="https://www.runoob.com/docker/docker-command-manual.html">Docker 命令大全</a></p>
</blockquote>

<p><img src="media/15820942246314/15820967152707.jpg" alt="" style="width:470px;"/></p>

<p>上面命令运行完成之后我们就已经成功运行了 jenkins 服务</p>

<p>我们可以打开地址 https://<code>centos服务器ip</code>:8080，可以看到 jenkins 的界面。</p>

<blockquote>
<p>这里的 <code>centos服务器ip</code> 就是你自己的服务器 ip，在这个教程里面，我们演示的服务器ip是 <code>172.22.0.4</code>。</p>
</blockquote>

<p><img src="media/15820942246314/15820961636958.jpg" alt="" style="width:1091px;"/></p>

<p>接下来我们需要获取jenkins初始化密码。</p>

<pre><code class="language-bash">docker exec -it jenkins-server \
    cat /var/jenkins_home/secrets/initialAdminPassword
</code></pre>

<p><img src="media/15820942246314/15820967619904.jpg" alt="" style="width:455px;"/></p>

<p>这里返回的一段字符串<code>6c46ccc61c804279a1017b743ab22aa4</code>就是我们需要的密码</p>

<p>输入密码之后，选择安装初始化的插件</p>

<p><img src="media/15820942246314/15820971428778.jpg" alt="" style="width:1013px;"/><br/>
<img src="media/15820942246314/15820972130894.jpg" alt="" style="width:997px;"/><br/>
插件安装好之后，系统会提示你设置用户名密码，当然你也可以不设置，直接用 admin 账号。重新进入 jenkins 界面。<br/>
<img src="media/15820942246314/15820988660511.jpg" alt="" style="width:689px;"/><br/>
点击输入账号里面，这里的密码就是上文获取到的 jenkins 初始化密码 <code>6c46ccc61c804279a1017b743ab22aa4</code>，点击登录，进入系统:<br/>
<img src="media/15820942246314/15820989609929.jpg" alt="" style="width:696px;"/></p>

<p>到此为止。jenkins 就算是安装好了</p>

<blockquote>
<p>我们如果想要卸载 jenkins 的话，只需要运行 <code>docker rm -f jenkins-server</code> 就可以了。</p>

<p>因为我们配置了 jenkins 数据持久化的目录 <code>/data/jenkins_home</code>， 只要这个文件夹还在，jenkins 配置就可以一直保留，下次想运行 jenkins 是，运行上文安装 jenkins 的 docker run 命令就可以恢复 docker 运行了。</p>
</blockquote>

<h3 id="toc_4">配置 jenkins 的 nodejs 插件</h3>

<p>安装完 jenkins 之后， 需要配置一下 nodejs 插件，后面才能让 jenkins 跑前端持续集成任务。</p>

<p>点击 <strong>系统管理</strong>  进入 jenkins 系统管理页面<br/>
<img src="media/15820942246314/15820991787999.jpg" alt="" style="width:315px;"/></p>

<p>点击 <strong>插件管理</strong> 进入插件管理<br/>
<img src="media/15820942246314/15821002348355.jpg" alt="" style="width:1021px;"/></p>

<p>进入 <strong>可选插件</strong> 选项卡，搜索并安装 <code>nodejs</code> 插件<br/>
<img src="media/15820942246314/15821180399210.jpg" alt="" style="width:1120px;"/></p>

<p>勾选 <code>安装完成后重启选项</code>，安装完插件之后 jenkins 会 <strong>自动重启</strong></p>

<p><img src="media/15820942246314/15821181345609.jpg" alt="" style="width:554px;"/></p>

<p>进入 <strong>全局工具配置</strong> 配置 nodejs 全局工具</p>

<p><img src="media/15820942246314/15821182607013.jpg" alt="" style="width:931px;"/></p>

<p>点击 <strong>新增 NodeJS</strong> 按键新增一个 nodejs 全局工具，勾选 <strong>自动安装</strong></p>

<p><img src="media/15820942246314/15821185123655.jpg" alt="15821185123655"/></p>

<p>注意:</p>

<ul>
<li>安装 nodejs 时需要安装两个 node 全局命令 <code>yarn</code> 和 <code>lerna</code>, 然后点击保存。</li>
</ul>

<h3 id="toc_5">测试 nodejs 插件</h3>

<p>我们安装完了 nodejs 插件之后，先回到首页 新建一个 jenkins 任务测试一下 nodejs。</p>

<p>点击 <strong>创建一个新任务</strong> 打开任务编辑界面<br/>
<img src="media/15820942246314/15821185754719.jpg" alt="" style="width:630px;"/><br/>
选择 <strong>构建自由风格的软件项目</strong>， 输入一个任务名字<code>test-node-demo</code>:<br/>
<img src="media/15820942246314/15821187127960.jpg" alt="" style="width:718px;"/></p>

<p>在构建环境这里选择我们刚刚配置的 nodejs 全局工具</p>

<p><img src="media/15820942246314/15821187744475.jpg" alt="" style="width:734px;"/></p>

<p>在构建这里 点击 <strong>增加构建步骤</strong> ，在弹出的选项中选择 <strong>执行shell</strong> ：</p>

<p><img src="media/15820942246314/15821188924270.jpg" alt="" style="width:359px;"/></p>

<p>在命令输入框中输入,然后点击保存按钮</p>

<pre><code class="language-text">node -e &#39;console.log(&quot;Hello Hzero!&quot;);&#39;
node --version
</code></pre>

<p><img src="media/15820942246314/15821190991233.jpg" alt="" style="width:758px;"/></p>

<p>然后点击这个任务的 <strong>立即构建</strong> 按钮</p>

<p>构建时，在下面会出现一个进度条，点击进度条 可以看执行的日志</p>

<p><img src="media/15820942246314/15821191550792.jpg" alt="" style="width:624px;"/></p>

<p>进入日志之后，将看到看到如下信息</p>

<p><img src="media/15820942246314/15821240438051.jpg" alt="" style="width:1061px;"/></p>

<p>到此，nodejs 插件测试成功！</p>

<h3 id="toc_6">安装 nginx</h3>

<p>centos 安装 nginx 也非常简单 ，一句命令 <code>sudo yum install nginx</code> 就能搞定， 但是这样安装 nginx 之后，配置管理和nginx升级都比较麻烦，所以我们这里用 docker 来安装 nginx 。</p>

<p>这里我们先定义两个文件目录，用来保存 nginx 的部署文件和 nginx 的配置</p>

<ul>
<li><code>/data/nginx/wwwroot</code> nginx 的部署文件夹</li>
<li><code>/data/nginx/nginx_hzero.conf</code> hzero 的 nginx 配置</li>
</ul>

<p>首先创建 nginx 数据持久化文件夹和 配置保存文件夹,</p>

<pre><code class="language-bash">sudo mkdir -p /data/nginx/wwwroot
sudo mkdir -p /data/nginx/cfg
</code></pre>

<p><img src="media/15820942246314/15821112605783.jpg" alt="" style="width:380px;"/></p>

<p>创建 nginx 配置文件, 保存在 <code>/data/nginx/cfg/nginx_hzero.conf</code></p>

<p>在命令行运行下面命令：</p>

<pre><code class="language-bash"># /data/nginx/cfg/nginx_hzero.conf

sudo -s # 进入root用户模式

cat &gt; /data/nginx/cfg/nginx_hzero.conf &lt;&lt; END

server {
    listen       80;
    server_name  localhost;
    root   /usr/share/nginx/html/dist;
    
    location / {
      try_files \$uri /index.html;
    }

    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html/dist;
    }

    # Media: images, icons, video, audio, HTC
    location ~* \.(jpg|jpeg|gif|png|ico|cur|gz|svg|svgz|mp4|ogg|ogv|webm|htc)$ {
    expires 1M;
    access_log off;
    add_header Cache-Control &quot;public&quot;;
    }

    # CSS and Javascript
    location ~* \.(css|js)$ {
    expires 1y;
    access_log off;
    add_header Cache-Control &quot;public&quot;;
    }
}

END

</code></pre>

<p><img src="media/15820942246314/15821112926505.jpg" alt="" style="width:590px;"/></p>

<p>运行nginx镜像</p>

<pre><code class="language-bash">docker run \
  -u root \
  --rm \
  --name nginx \
  -d \
  -p 40080:80 \
  -v /data/nginx/cfg/:/etc/nginx/conf.d/ \
  -v /data/nginx/wwwroot/:/usr/share/nginx/html/ \
  nginx
</code></pre>

<p><img src="media/15820942246314/15821113566156.jpg" alt="" style="width:458px;"/></p>

<p>添加测试文件</p>

<pre><code class="language-bash">sudo mkdir -p /data/nginx/wwwroot/dist
sudo echo Hello Nginx &gt; /data/nginx/wwwroot/dist/test.txt
</code></pre>

<p>测试 nginx 运行效果</p>

<pre><code class="language-bash">curl http://172.22.0.4:40080/test.txt
</code></pre>

<p><img src="media/15820942246314/15821113849756.jpg" alt="" style="width:472px;"/></p>

<p>如果能显示 Hello Nginx 说明 nginx 安装成功</p>

<h3 id="toc_7">配置 jenkins 持续集成任务</h3>

<h4 id="toc_8">构建</h4>

<p>配置 jenkins 任务之前，我们先准备一个 hzero-front 工程的代码仓库地址：<br/>
<code>https://code.choerodon.com.cn/hft/hzero-front-demo.git</code></p>

<p>回到 jenkins 首页 新建一个 jenkins 任务。</p>

<p>点击 <strong>创建一个新任务</strong> 打开任务编辑界面<br/>
<img src="media/15820942246314/15821185754719.jpg" alt="" style="width:630px;"/><br/>
选择 <strong>构建自由风格的软件项目</strong>， 输入一个任务名字<code>hzero-front-ci</code>:</p>

<p>点击保存后会进入任务编辑页面：</p>

<p><img src="media/15820942246314/15821309390419.jpg" alt="" style="width:1054px;"/></p>

<p>在任务编辑界面的源代码管理 选择 git 选项<br/>
安装图片填写下面信息：</p>

<ul>
<li>Repository URL： 输入代码库地址（<code>https://code.choerodon.com.cn/hft/hzero-front-demo.git</code>）</li>
<li>Credentials：添加 code.choerodon.com.cn 的账号密码</li>
</ul>

<p>在编译环境设置里面勾选上 nodejs 工具。</p>

<p><img src="media/15820942246314/15821311690599.jpg" alt="" style="width:980px;"/></p>

<p>添加一个构建步骤 <code>执行 shell</code><br/>
<img src="media/15820942246314/15821312331832.jpg" alt="" style="width:515px;"/></p>

<p>输入下面的 shell 脚本:</p>

<pre><code class="language-bash">yarn install

cat &gt; src/config/.env.production.yml.local  &lt;&lt; END
BASE_PATH: /
PLATFORM_VERSION: SAAS
CLIENT_ID: localhost
GENERATE_SOURCEMAP: false
API_HOST: http://backend.hft.jajabjbj.top
WEBSOCKET_HOST: ws://ws.hft.jajabjbj.top
SKIP_TS_CHECK_IN_START: false
ENABLE_VUE_SUPPORT: false
SKIP_ESLINT_CHECK_IN_START: false
END
yarn run build:production
tar -zcf dist.tar.gz ./dist
</code></pre>

<p><img src="media/15820942246314/15821804456276.jpg" alt="" style="width:961px;"/></p>

<p>点击保存。</p>

<p>下一步然后点击 立即构建，这里会构建并生成 dist 文件</p>

<p><img src="media/15820942246314/15821313783678.jpg" alt="" style="width:711px;"/></p>

<h4 id="toc_9">部署</h4>

<p>构建完成生成了 dist 文件，下一步我们要把 构建生成的文件部署到 nginx 的 www 目录 <code>/data/nginx/wwwroot</code></p>

<p>接下来我们配置一下 dist 部署流程</p>

<p>部署通常都是指把构建好的 dist 文件部署到远程服务器，部署的方法有很多，比如 ftp、 构建 docker等、ssh ， 这里我们演示一下如何通过 ssh 远程部署 dist 文件到 nginx 服务器上。</p>

<p>首先我们需要安装一个 jenkins 插件 <code>Publish Over SSH</code>。</p>

<p><img src="media/15820942246314/15821775655990.jpg" alt="" style="width:814px;"/></p>

<p>插件的安装方法上文中有提到，这里就不再赘述。</p>

<p>安装好之后，打开 <strong>系统管理</strong> &gt; <strong>配置中心</strong> <br/>
<img src="media/15820942246314/15821779486366.jpg" alt="" style="width:793px;"/></p>

<p>把 nginx 服务器的信息 添加到 <code>Publish Over SSH</code> 的 ssh 配置。</p>

<p><img src="media/15820942246314/15821778504635.jpg" alt="" style="width:1413px;"/></p>

<p>对关键配置项的解释：</p>

<ul>
<li><strong>name</strong>: 给服务器起一个名字，在后面设置任务时会用到这个名字。</li>
<li><strong>Hostname</strong>: sshd 服务器的ip地址, 也就是 nginx 服务器ip地址。</li>
<li><strong>Remote Directory</strong>: 这里是设置服务器上传目录，由于在 jenkins 任务里面可以配置， 为了不引起误解，这里设置成 <code>/</code> 就可以了。</li>
<li><strong>Passphrase/Password</strong>: 这里是服务器 ssh 登录密码，如果是通过私钥连接服务器，可以不输入密码。</li>
<li><strong>Key</strong>: 这里是服务器的私钥文件内容。如果你是通过 密码 连接 ssh 服务器的话，可以不填私钥。</li>
</ul>

<p>然后打开我们刚刚创建的 jenkins 任务，点击配置按钮，进入任务编辑页面。</p>

<p><img src="media/15820942246314/15821318511535.jpg" alt="" style="width:500px;"/></p>

<p>到 <code>Post-build Actions</code> 配置部分添加一个 构建后的部署流程。<br/>
<img src="media/15820942246314/15821781519316.jpg" alt="" style="width:651px;"/></p>

<p>然后输入相应的配置</p>

<p><img src="media/15820942246314/15821781898781.jpg" alt="" style="width:968px;"/></p>

<p>对关键配置项的解释：</p>

<ul>
<li><strong>name</strong>: 上文在 <strong>系统管理</strong> &gt; <strong>配置中心</strong> 中配置的 ssh 服务器名字。</li>
<li><strong>Source files</strong>: 制定上传到服务器的文件，注意这里的文件是上面我们配置的构建脚本产生的文件。</li>
<li><strong>Remote directory</strong>: 这个是上传到远程服务器位置，上传上去的文件还是一个压缩包，后面还有在这个目录下执行节约命令</li>
<li><strong>Exec command</strong>: 在远程服务器上执行的 shell 命令</li>
</ul>

<pre><code class="language-bash">cd /data/nginx/wwwroot
sudo rm -rf dist
sudo tar -zxf dist.tar.gz
</code></pre>

<p>然后点击保存，再构建一次。<br/>
<img src="media/15820942246314/15821793949244.jpg" alt="" style="width:1156px;"/></p>

<p>看下构建日志。提示发布成功。</p>

<p><img src="media/15820942246314/15821794072373.jpg" alt="" style="width:525px;"/></p>

<p>然后我们可以 在浏览器输入一下 nginx 服务器地址， 试试服务是否成功。</p>

<h4 id="toc_10">配置自动持续集成</h4>

<p>如果想实现完全自动的持续集成，还需要配置一个地方</p>

<p>打开任务配置设置一个触发选项 <code>Poll SCM</code>，设置 <code>Schedule</code> 规则为: <code>H/15 * * * *</code></p>

<p><img src="media/15820942246314/15821800915046.jpg" alt="" style="width:1229px;"/></p>

<p>这里配置项的意思是：每 15 分钟检查一次 git 源代码仓库是否有新的变更，如果有就执行构建任务。</p>

<blockquote>
<p><a href="https://tool.lu/crontab/">关于 cronjob 表达式的学习</a></p>
</blockquote>

<h2 id="toc_11">jenkins + gh-pages 实现持续集成</h2>

<p>如果没有 nginx 服务器，我们也可以把 dist 部署到 gh-pages 上。</p>

<p>考虑到 github 的 gh-pages 网速在国内有些慢， 本教程就用国内的 gitee 做一个示范。</p>

<blockquote>
<p>gitee网站: <a href="https://gitee.com/">https://gitee.com/</a></p>
</blockquote>

<p>首先我们在 gitee 上面注册好账号，然后再创建一个代码仓库。</p>

<p><img src="media/15820942246314/15821819693074.jpg" alt="" style="width:786px;"/></p>

<p>我们可以得到一个代码仓库地址：<code>https://gitee.com/hzero-cli/hzero-front-demo-dist.git</code></p>

<p>部署到 gh-pages 上面我们需要用到一个 npm 工具</p>

<ul>
<li>gh-pages: <a href="https://www.npmjs.com/package/gh-pages">gh-pages 文档地址</a></li>
</ul>

<p>把这个工具配置到 nodejs 的全局工具上面：</p>

<p><img src="media/15820942246314/15821830690021.jpg" alt="" style="width:910px;"/></p>

<p>配置好工具之后，开始创建 jenkins 任务。</p>

<p>为了简便，我们基于之前的持续集成任务创建一个新的持续集成任务。</p>

<p>点击 <strong>新建任务</strong> 按钮 <br/>
<img src="media/15820942246314/15821821404080.jpg" alt="" style="width:670px;"/></p>

<p>为任务起一个新名字： <code>hzero-front-ci-gitee</code></p>

<p><img src="media/15820942246314/15821822551782.jpg" alt="" style="width:1074px;"/></p>

<p>注意这里我们要选择 <strong>Copy from</strong>, 输入前面创建的任务名字：<code>hzero-front-ci</code></p>

<p>点击 ok ，进入构建配置部分。<br/>
<img src="media/15820942246314/15821827533997.jpg" alt="" style="width:1201px;"/></p>

<p>修改构建脚本，添加 gh-pages 部署命令：</p>

<pre><code class="language-bash">git config --global user.email &quot;you@example.com&quot;
git config --global user.name &quot;Your Name&quot;
gh-pages -r https://hzero-cli:abcd1234@gitee.com/hzero-cli/hzero-front-demo-dist.git   -b gh-pages   -d dist
</code></pre>

<p>然后删除从 <code>hzero-front-ci</code> 任务复制过来的 ssh部署 配置</p>

<p>保存，点击立即构建。</p>

<p>这样构建生成的 dist 就可以发布到 gitee 代码仓库了。</p>

<p><img src="media/15820942246314/15821861555894.jpg" alt="" style="width:820px;"/></p>

<p>这里会多一个分支，就是我们部署上去的文件。</p>

<p>接下来我们需要给 gitee 代码仓库设置一下 gh-pages 的 http 服务。</p>

<p>点击 <strong>服务</strong> &gt; <strong>Gitee Pages</strong></p>

<p><img src="media/15820942246314/15821862428010.jpg" alt="" style="width:973px;"/></p>

<p>设置部署分支为 gh-pages ,然后点击开始按钮</p>

<p>运行成功之后， 我们得到一个 http 访问地址：<code>http://hzero-cli.gitee.io/hzero-front-demo-dist/</code></p>

<p>但是访问时， console 还是会报错: </p>

<p><img src="media/15820942246314/15821864231685.jpg" alt="" style="width:1147px;"/></p>

<p>这是因为 gitee 的 gh-pages 提供的 http 地址有一个根路径(public_path)。</p>

<p>所以我们修改一下构建的环境变量，打开任务的构建配置修改环境变量：</p>

<pre><code class="language-yaml">PUBLIC_URL: /hzero-front-demo-dist/
BASE_PATH: /hzero-front-demo-dist/
</code></pre>

<p>注意一下 BASE_PATH 和 PUBLIC_URL 的区别。</p>

<ul>
<li><strong>PUBLIC_URL</strong> : 会影响资源加载路径，在资源加载路径前面加一层根路径。</li>
<li><strong>BASE_PATH</strong> : 会影响浏览器的地址栏根路径。在地址栏显示的地址上加一层根路径。</li>
</ul>

<p>虽然 BASE_PATH 不会影响资源加载路径，但是我们进入页面之后刷新，必须浏览器的地址栏是以 <code>http://hzero-cli.gitee.io/hzero-front-demo-dist/</code> 为前缀，所以要加一下 BASE_PATH 的配置。</p>

<p>清除构建缓存，然后点击重新构建<br/>
<img src="media/15820942246314/15821843422297.jpg" alt="" style="width:369px;"/></p>

<blockquote>
<p>注意: 由于 hzero-cli 默认开启了增量更新， 所以修改了环境变量之后，需要清空当前构建任务的缓存。</p>
</blockquote>

<p>如果你访问页面，第二次刷新还会有 404 的问题</p>

<p>这是因为我们 gh-pages 和 nginx 配置 不一样。<br/>
nginx 上面配置了一段代码： </p>

<pre><code class="language-nginx">    location / {
      try_files $uri /index.html;
    }
</code></pre>

<p>有了这段代码， 服务器就会路径找不到的http请求重新定位到 /index.html</p>

<p>但是 gh-pages 的原理是: 如果当前 http 请求路径定位不到仓库的文件，就返回 404.html 的内容。</p>

<p>所以为了能让页面正常显示，我们还需要在脚本构建完成之后 加一行代码 <code>cp dist/index.html dist/404.html</code><br/>
让浏览器能获取到 index.html 。</p>

<p>最后脚本修改如下：</p>

<pre><code class="language-bash">yarn install
cat &gt; src/config/.env.production.local.yml  &lt;&lt; END
PUBLIC_URL: /hzero-front-demo-dist/
BASE_PATH: /hzero-front-demo-dist/

PLATFORM_VERSION: SAAS
CLIENT_ID: localhost
GENERATE_SOURCEMAP: false
API_HOST: http://backend.hft.jajabjbj.top
WEBSOCKET_HOST: ws://ws.hft.jajabjbj.top
SKIP_TS_CHECK_IN_START: false
ENABLE_VUE_SUPPORT: false
SKIP_ESLINT_CHECK_IN_START: false

END

yarn run build:production
cp dist/index.html dist/404.html

git config --global user.email &quot;you@example.com&quot;
git config --global user.name &quot;Your Name&quot;
gh-pages-clean
gh-pages -r https://hzero-cli:abcd1234@gitee.com/hzero-cli/hzero-front-demo-dist.git \
  -b gh-pages \
  -d dist
</code></pre>

<p>清除构建缓存之后，点击重新构建, 构建成功会自动部署，部署完成之后就可以访问： <a href="http://hzero-cli.gitee.io/hzero-front-demo-dist">http://hzero-cli.gitee.io/hzero-front-demo-dist</a> 了</p>

<h2 id="toc_12">gitlab-ci + choerodon 云平台的 k8s 环境持续集成</h2>

<p>关于 gitlab-ci 的持续集成，hzero-cli 创建的项目中包括了 gitlab-ci 脚本，不需要进行特殊设置。</p>

<pre><code class="language-yaml">image: registry.cn-hangzhou.aliyuncs.com/choerodon-tools/cifront:0.7.0

stages:
  - node_build
  - docker_build

node_build:
  image: registry.cn-hangzhou.aliyuncs.com/hzero-cli/cifront:0.0.1
  stage: node_build
  script:
    - node_module
    - node_build
  only:
    - master

docker_build:
  image: registry.cn-hangzhou.aliyuncs.com/choerodon-tools/cibase:0.7.0
  stage: docker_build
  script:
    - docker_build
    - chart_build
  only:
    - master

.auto_devops: &amp;auto_devops |
  http_status_code=`curl -o .auto_devops.sh -s -m 10 --connect-timeout 10 -w %{http_code} &quot;${CHOERODON_URL}/devops/ci?token=${Token}&amp;type=front&quot;`
  if [ &quot;$http_status_code&quot; != &quot;200&quot; ]; then
    cat .auto_devops.sh
    exit 1
  fi
  source .auto_devops.sh
  export TEMP_DIR=/cache/${CI_PROJECT_NAME}-${CI_PROJECT_ID}-${CI_COMMIT_REF_NAME}-${CI_COMMIT_SHA}-front
  function node_module(){
      # mkdir -p /cache/${CI_PROJECT_NAME}-${CI_PROJECT_ID}-${CI_COMMIT_REF_NAME}-${CI_COMMIT_SHA}
      mkdir -p $TEMP_DIR
      if [ -f &quot;$TEMP_DIR/node_modules.tar.gz&quot; ]; then
        tar -zxf $TEMP_DIR/node_modules.tar.gz
        chmod -R 777 node_modules
      else
        yarn
        tar -zcf $TEMP_DIR/node_modules.tar.gz ./node_modules
      fi
  }

  function node_build(){
      if [ -f &quot;$TEMP_DIR/dist.tar.gz&quot; ]; then
        tar -zxf $TEMP_DIR/dist.tar.gz
      fi
      if [ -f &quot;$TEMP_DIR/dll.tar.gz&quot; ]; then
        tar -zxf $TEMP_DIR/dll.tar.gz
      fi
      chmod -R 777 node_modules
      yarn build:production
      # cp -r dist /cache/${CI_PROJECT_NAME}-${CI_PROJECT_ID}-${CI_COMMIT_REF_NAME}-${CI_COMMIT_SHA}/dist
      tar -zcf $TEMP_DIR/dist.tar.gz ./dist
      tar -zcf $TEMP_DIR/dll.tar.gz ./dll

  }

  function docker_build(){
      # cp -r /cache/${CI_PROJECT_NAME}-${CI_PROJECT_ID}-${CI_COMMIT_REF_NAME}-${CI_COMMIT_SHA}/* ${1:-&quot;.&quot;}
      tar -zxf $TEMP_DIR/dist.tar.gz
      docker login -u ${DOCKER_USER} -p ${DOCKER_PWD} ${DOCKER_REGISTRY}
      docker build --pull -t ${DOCKER_REGISTRY}/${GROUP_NAME}/${PROJECT_NAME}:${CI_COMMIT_TAG} ${1:-&quot;.&quot;}
      docker push ${DOCKER_REGISTRY}/${GROUP_NAME}/${PROJECT_NAME}:${CI_COMMIT_TAG}
      echo &quot;${DOCKER_REGISTRY}/${GROUP_NAME}/${PROJECT_NAME}:${CI_COMMIT_TAG}&quot;
  }

before_script:
  - *auto_devops
</code></pre>

<p>更新代码仓库内容之后会自动触发 gitlab cicd 的 pipeline</p>

<p>这里注意一下：更新了 PUBLIC_URL 配置之后，还是需要执行一下 <strong>构建缓存清空</strong>:<br/>
<img src="media/15820942246314/15821868715510.jpg" alt="" style="width:1436px;"/></p>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[HZERO全新脚手架HZERO-CLI不容错过！]]></title>
    <link href="https://blog.jajabjbj.top/vk58rd.html"/>
    <updated>2020-02-10T04:04:21+08:00</updated>
    <id>https://blog.jajabjbj.top/vk58rd.html</id>
    <content type="html"><![CDATA[
<p><a name="bb0e0fe2"></a></p>

<h2 id="toc_0">全新的技术架构！</h2>

<p><a name="8fc8c169"></a></p>

<h2 id="toc_1">基于 QIANKUN &amp; NEST-CLI &amp; LERNA 升级了整体架构。</h2>

<p><img src="https://cdn.nlark.com/yuque/0/2020/png/155608/1582004751429-519b14de-bc70-4236-a5f9-c4ff01506307.png#align=left&amp;display=inline&amp;height=980&amp;originHeight=980&amp;originWidth=1146&amp;size=0&amp;status=done&amp;style=none&amp;width=1146" alt=""/></p>

<p>我们把一个前端项目分成以下几个部分：</p>

<ul>
<li>hzero-boot: 定义了一组 api 实现 页面整体的布局、全局配置、规范子模块的页面路由组合方式等、被父工程和子工程依赖，父工程也可以直接当 hzero-boot。</li>
<li>hzero-webpack-script: 封装 webpack 构建的配置和 webpack 脚本。</li>
<li>hzero-cli: 命令行工具，这里可以封装一些处理代码的小工具，比如代码的 hzero 多语言处理工具等。</li>
<li>hzero-schemas: 这里是用来页面可以模块的模板，模块可以是基于 react 或者 vue。</li>
<li>微前端子模块: 基于组件化的思想， 子模块也是可以一个组成部分。</li>
</ul>

<p>上面的每个部分也可组装可替换。</p>

<p>比如我们的视图布局想要换成 aliyun console 的样式，我们只需要重新开发一个 hzero-boot-aliyun-console 把它替换掉 hzero-boot 就可以了。</p>

<p><a name="759622d1"></a></p>

<h2 id="toc_2">丰富的功能</h2>

<ol>
<li>代码多语言扫描</li>
<li>支持增量编译</li>
<li>lerna 的多个子模块管理</li>
<li>代码生成器</li>
<li>typescript 支持</li>
</ol>

<p><a name="08b074cc"></a></p>

<h2 id="toc_3">解决了 HZERO 实施当中一系列痛点问题！</h2>

<ol>
<li>文件过多导致 webpack 编译的内存溢出。</li>
<li>不必要构建时间增加：每次构建都需要全部文件扫描编译，比如在一千个文件中，只修改了其中一个文件一行代码，每次重新发布版本时，也需要全部编译一次。</li>
<li>多团队并行开发时合并代码容易冲突。</li>
</ol>

<p><a name="1a138b8a"></a></p>

<h1 id="toc_4">话不多说，我们如何进行操作使用呢？</h1>

<p>下面将示范一下 hzero-cli 的简单使用流程</p>

<p><a name="8cbd8915"></a></p>

<h3 id="toc_5">安装 hzero-cli</h3>

<blockquote>
<p><strong>注意</strong>：hzero-cli 需要安装 node 10 以上的版本，这里建议大家安装 node 最新稳定版本(lts 版)。</p>
</blockquote>

<p>全局安装 hzero-cli 命令行工具</p>

<pre><code class="language-bash">npm install -g hzero-cli --registry http://nexus.saas.hand-china.com/content/groups/hzero-npm-group # 安装 hzero-cli 工具
</code></pre>

<p><a name="8aae0c6b"></a></p>

<h3 id="toc_6">生成项目</h3>

<pre><code class="language-bash">hzero-cli new sample # 创建 hzero 前端项目
cd sample &amp;&amp; hzero-cli info # 查看当前工程信息
</code></pre>

<p><a name="c8f939d5"></a></p>

<h3 id="toc_7">创建子模块</h3>

<pre><code class="language-bash">npx hzero-cli g sub-module test-module  # 创建一个名字为 test-module 的子模块
</code></pre>

<p>运行完成之后，当前项目目录会多一个文件夹 <code>packages/sample-test-module</code>, 这里 hzero 中规定所有子模块的名字前缀默认都是父工程的名字。</p>

<p><a name="0fc757b5"></a></p>

<h3 id="toc_8">创建页面</h3>

<p>进入子模块目录：</p>

<pre><code class="language-bash">cd packages/sample-test-module
</code></pre>

<p>在子模块下创建一个页面：</p>

<pre><code class="language-bash">npx hzero-cli g simple-page test-page # 创建一个名字为 test-page 的页面
</code></pre>

<p>创建完成生成一个简单页面,生成 <code>packages/sample-test-module/src/routes/test-page</code> 文件夹。</p>

<p><a name="9acb1e51"></a></p>

<h3 id="toc_9">启动调试</h3>

<pre><code class="language-bash">yarn start
</code></pre>

<p>开发调试可以可以在父项目目录运行，也可以在子模块目录运行。<br />区别在于:</p>

<ul>
<li>父项目目录运行调试只是调试子模块的加载情况，调试的是编译后的子模块数据，如果想调试那个子模块，先用按需增量编译那个子模块</li>
<li>子模块目录运行 调试，调试的范围是只是当前子模块下的页面</li>
</ul>

<p>当开始编译后会自动打开浏览器，通过 localhost:8000，查看页面效果。</p>

<p><a name="a98c6810"></a></p>

<h3 id="toc_10">全量编译</h3>

<p>进入父工程目录</p>

<pre><code class="language-bash">yarn run build
</code></pre>

<p>全量编译会删掉之前编译的所有代码。<br />全量编译会生成 dist 文件夹， 所有子模块的代码会编译到 <code>dist/packages</code>下以子模块的名字命名的文件夹。</p>

<p><a name="4c9ce866"></a></p>

<h3 id="toc_11">增量编译</h3>

<p>进入父工程目录</p>

<pre><code class="language-bash">yarn run build:ms sample-test-module # 增量编译名字为 sample-test-module 的模块
</code></pre>

<p>注意：增量编译时， 如果父工程已经编译过， 这里不会重新编译父项目文件。<br />增量编译重新生成 <code>dist/packages</code>下以子模块的名字命名的文件夹，以及 packages 文件下的 microConfig.json 索引文件。</p>

<p>更多操作 👉👉👉 <a href="http://hzero-front-docs.hft.jajabjbj.top/zh/docs/quick-start/init-project/">视频教程</a></p>

<p><a name="930d842b"></a></p>

<h1 id="toc_12">接下来我们做什么</h1>

<blockquote>
<p>hzero-cli 是 hzero 前端工程的构建工具。hzero-cli 是 目前hzero 前端工程的命令行工具，通过 hzero-cli 可以创建微前端架构的前端项目脚手架，创建子模块，创建页面。</p>
</blockquote>

<ul>
<li><p>hzero-cli 还在开发中， 后续讲支持更多功能：</p></li>
</ul>

<ol>
<li>代码模板兼容阿里 ui更多块市场生态</li>
<li>增强微前端支持 支持热更新加载子应用</li>
<li>支持可视化 hzero-cli 配置界面</li>
</ol>

<p>技术小哥们正在赶制中，敬请期待！！</p>

<p><a name="56dfdb14"></a></p>

<h2 id="toc_13">其他资料</h2>

<ul>
<li><a href="https://code.choerodon.com.cn/hft/hzero-cli.git">hzero-cli 源码</a></li>
<li><a href="https://code.choerodon.com.cn/hft/hzero-front-demo.git">hzero</a><a href="https://code.choerodon.com.cn/hft/hzero-front-demo.git"> </a><a href="https://code.choerodon.com.cn/hft/hzero-front-demo.git">项目Demo</a></li>
<li><a href="http://hzero-front-docs.hft.jajabjbj.top/zh/docs/quick-start/init-project/">hzero-cli</a><a href="http://hzero-front-docs.hft.jajabjbj.top/zh/docs/quick-start/init-project/"> </a><a href="http://hzero-front-docs.hft.jajabjbj.top/zh/docs/quick-start/init-project/">使用教程</a></li>
<li><a href="https://zhuanlan.zhihu.com/p/78362028">qiankun</a></li>
</ul>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Markdown 语法和 MWeb 写作使用说明]]></title>
    <link href="https://blog.jajabjbj.top/markdown-yu-fa-he-mweb-xie-zuo.html"/>
    <updated>2020-02-10T03:27:15+08:00</updated>
    <id>https://blog.jajabjbj.top/markdown-yu-fa-he-mweb-xie-zuo.html</id>
    <content type="html"><![CDATA[
<h2 id="toc_0">Markdown 的设计哲学</h2>

<blockquote>
<p>Markdown 的目標是實現「易读易写」。<br/>
不過最需要強調的便是它的可讀性。一份使用 Markdown 格式撰寫的文件應該可以直接以純文字發佈，並且看起來不會像是由許多標籤或是格式指令所構成。<br/>
Markdown 的語法有個主要的目的：用來作為一種網路內容的<em>寫作</em>用語言。</p>
</blockquote>

<span id="more"></span><!-- more -->

<h2 id="toc_1">本文约定</h2>

<p>如果有写 <code>效果如下：</code>， 在 MWeb 编辑状态下只有用 <code>CMD + 4</code> 或 <code>CMD + R</code> 预览才可以看效果。</p>

<h2 id="toc_2">标题</h2>

<p>Markdown 语法：</p>

<pre><code class="language-text"># 第一级标题 `&lt;h1&gt;` 
## 第二级标题 `&lt;h2&gt;` 
###### 第六级标题 `&lt;h6&gt;` 
</code></pre>

<p>效果如下：</p>

<h1 id="toc_3">第一级标题 <code>&lt;h1&gt;</code></h1>

<h2 id="toc_4">第二级标题 <code>&lt;h2&gt;</code></h2>

<h6 id="toc_5">第六级标题 <code>&lt;h6&gt;</code></h6>

<h2 id="toc_6">强调</h2>

<p>Markdown 语法：</p>

<pre><code class="language-text">*这些文字会生成`&lt;em&gt;`*
_这些文字会生成`&lt;u&gt;`_

**这些文字会生成`&lt;strong&gt;`**
__这些文字会生成`&lt;strong&gt;`__
</code></pre>

<p>在 MWeb 中的快捷键为： <code>CMD + U</code>、<code>CMD + I</code>、<code>CMD + B</code><br/>
效果如下：</p>

<p><em>这些文字会生成<code>&lt;em&gt;</code></em><br/>
<u>这些文字会生成<code>&lt;u&gt;</code></u></p>

<p><strong>这些文字会生成<code>&lt;strong&gt;</code></strong><br/>
<strong>这些文字会生成<code>&lt;strong&gt;</code></strong></p>

<h2 id="toc_7">换行</h2>

<p>四个及以上空格加回车。<br/>
如果不想打这么多空格，只要回车就为换行，请勾选：<code>Preferences</code> - <code>Themes</code> - <code>Translate newlines to &lt;br&gt; tags</code></p>

<h2 id="toc_8">列表</h2>

<h3 id="toc_9">无序列表</h3>

<p>Markdown 语法：</p>

<pre><code class="language-text">* 项目一 无序列表 `* + 空格键`
* 项目二
    * 项目二的子项目一 无序列表 `TAB + * + 空格键`
    * 项目二的子项目二
</code></pre>

<p>在 MWeb 中的快捷键为： <code>Option + U</code><br/>
效果如下：</p>

<ul>
<li>项目一 无序列表 <code>* + 空格键</code></li>
<li>项目二
<ul>
<li>项目二的子项目一 无序列表 <code>TAB + * + 空格键</code></li>
<li>项目二的子项目二</li>
</ul></li>
</ul>

<h3 id="toc_10">有序列表</h3>

<p>Markdown 语法：</p>

<pre><code class="language-text">1. 项目一 有序列表 `数字 + . + 空格键`
2. 项目二 
3. 项目三
    1. 项目三的子项目一 有序列表 `TAB + 数字 + . + 空格键`
    2. 项目三的子项目二
</code></pre>

<p>效果如下：</p>

<ol>
<li>项目一 有序列表 <code>数字 + . + 空格键</code></li>
<li>项目二 </li>
<li>项目三
<ol>
<li>项目三的子项目一 有序列表 <code>TAB + 数字 + . + 空格键</code></li>
<li>项目三的子项目二</li>
</ol></li>
</ol>

<h3 id="toc_11">列表中嵌入代码块语法</h3>

<pre><code class="language-text">1. 项目一 有序列表 `数字 + . + 空格键`

    列表中嵌入代码块必须前后空一行，如这个写法

    ```js
    function fancyAlert(arg) {
      if(arg) {
        $.facebox({div:&#39;#foo&#39;})
      }
    }
    ```

    其他文本。

2. 项目二
</code></pre>

<h3 id="toc_12">任务列表（Task lists）</h3>

<p>Markdown 语法：</p>

<pre><code class="language-text">- [ ] 任务一 未做任务 `- + 空格 + [ ]`
- [x] 任务二 已做任务 `- + 空格 + [x]`
</code></pre>

<p>效果如下：</p>

<ul>
<li class="task-list-item"><input disabled="disabled" type="checkbox" /> 任务一 未做任务 <code>- + 空格 + [ ]</code></li>
<li class="task-list-item"><input disabled="disabled" type="checkbox" checked /> 任务二 已做任务 <code>- + 空格 + [x]</code></li>
</ul>

<h2 id="toc_13">图片</h2>

<p>Markdown 语法：</p>

<pre><code class="language-text">![GitHub set up](http://zh.mweb.im/asset/img/set-up-git.gif)
格式: ![Alt Text](url)
</code></pre>

<p><code>Control + Shift + I</code> 可插入Markdown语法。<br/>
如果是 MWeb 的文档库中的文档，还可以用拖放图片、<code>CMD + V</code> 粘贴、<code>CMD + Option + I</code> 导入这三种方式来增加图片。<br/>
效果如下：</p>

<p><img src="http://zh.mweb.im/asset/img/set-up-git.gif" alt="GitHub set up"/></p>

<p>MWeb 引入的特别的语法来设置图片宽度，方法是在图片描述后加 <code>-w + 图片宽度</code> 即可，比如说要设置上面的图片的宽度为 140，语法如为 <code>![GitHub-w140](set-up-git.gif)</code>：</p>

<p><img src="http://zh.mweb.im/asset/img/set-up-git.gif" alt="GitHub set up" style="width:140px;"/></p>

<h2 id="toc_14">链接</h2>

<p>Markdown 语法：</p>

<pre><code class="language-text">email &lt;example@example.com&gt;
[GitHub](http://github.com)
自动生成连接  &lt;http://www.github.com/&gt;
</code></pre>

<p><code>Control + Shift + L</code> 可插入Markdown语法。<br/>
如果是 MWeb 的文档库中的文档，拖放或<code>CMD + Option + I</code> 导入非图片时，会生成连接。<br/>
效果如下：</p>

<p>Email 连接： <a href="mailto:example@example.com">example@example.com</a><br/>
<a href="http://github.com">连接标题Github网站</a><br/>
自动生成连接像： <a href="http://www.github.com/">http://www.github.com/</a> 这样</p>

<h2 id="toc_15">区块引用</h2>

<p>Markdown 语法：</p>

<pre><code class="language-text">某某说:
&gt; 第一行引用
&gt; 第二行费用文字
</code></pre>

<p><code>CMD + Shift + B</code> 可插入Markdown语法。<br/>
效果如下：</p>

<p>某某说:</p>

<blockquote>
<p>第一行引用<br/>
第二行费用文字</p>
</blockquote>

<h2 id="toc_16">行内代码</h2>

<p>Markdown 语法：</p>

<pre><code class="language-text">像这样即可：`&lt;addr&gt;` `code`
</code></pre>

<p><code>CMD + K</code> 可插入Markdown语法。<br/>
效果如下：</p>

<p>像这样即可：<code>&lt;addr&gt;</code> <code>code</code></p>

<h2 id="toc_17">多行或者一段代码</h2>

<p>Markdown 语法：</p>

<pre><code class="language-text">```js
function fancyAlert(arg) {
  if(arg) {
    $.facebox({div:&#39;#foo&#39;})
  }

}
```
</code></pre>

<p><code>CMD + Shift + K</code> 可插入Markdown语法。<br/>
效果如下：</p>

<pre><code class="language-javascript">function fancyAlert(arg) {
    if(arg) {
        $.facebox({div:&#39;#foo&#39;})
    }

}
</code></pre>

<h2 id="toc_18">顺序图或流程图</h2>

<p>Markdown 语法：</p>

<pre><code class="language-text">```sequence
张三-&gt;李四: 嘿，小四儿, 写博客了没?
Note right of 李四: 李四愣了一下，说：
李四--&gt;张三: 忙得吐血，哪有时间写。
```

```flow
st=&gt;start: 开始
e=&gt;end: 结束
op=&gt;operation: 我的操作
cond=&gt;condition: 确认？

st-&gt;op-&gt;cond
cond(yes)-&gt;e
cond(no)-&gt;op
```
</code></pre>

<p>效果如下（ <code>Preferences</code> - <code>Themes</code> - <code>Enable sequence &amp; flow chart</code> 才会看到效果 ）：</p>

<pre><code class="language-sequence">张三-&gt;李四: 嘿，小四儿, 写博客了没?
Note right of 李四: 李四愣了一下，说：
李四--&gt;张三: 忙得吐血，哪有时间写。
</code></pre>

<pre><code class="language-flow">st=&gt;start: 开始
e=&gt;end: 结束
op=&gt;operation: 我的操作
cond=&gt;condition: 确认？

st-&gt;op-&gt;cond
cond(yes)-&gt;e
cond(no)-&gt;op
</code></pre>

<p>更多请参考：<a href="http://bramp.github.io/js-sequence-diagrams/">http://bramp.github.io/js-sequence-diagrams/</a>, <a href="http://adrai.github.io/flowchart.js/">http://adrai.github.io/flowchart.js/</a></p>

<h2 id="toc_19">表格</h2>

<p>Markdown 语法：</p>

<pre><code class="language-txt">第一格表头 | 第二格表头
--------- | -------------
内容单元格 第一列第一格 | 内容单元格第二列第一格
内容单元格 第一列第二格 多加文字 | 内容单元格第二列第二格
</code></pre>

<p>效果如下：</p>

<table>
<thead>
<tr>
<th>第一格表头</th>
<th>第二格表头</th>
</tr>
</thead>

<tbody>
<tr>
<td>内容单元格 第一列第一格</td>
<td>内容单元格第二列第一格</td>
</tr>
<tr>
<td>内容单元格 第一列第二格 多加文字</td>
<td>内容单元格第二列第二格</td>
</tr>
</tbody>
</table>

<h2 id="toc_20">删除线</h2>

<p>Markdown 语法：</p>

<pre><code class="language-text">加删除线像这样用： ~~删除这些~~
</code></pre>

<p>效果如下：</p>

<p>加删除线像这样用： <del>删除这些</del></p>

<h2 id="toc_21">分隔线</h2>

<p>以下三种方式都可以生成分隔线：</p>

<pre><code class="language-text">***

*****

- - -
</code></pre>

<p>效果如下：</p>

<hr/>

<hr/>

<hr/>

<h2 id="toc_22">MathJax</h2>

<p>Markdown 语法：</p>

<pre><code class="language-text">块级公式：
$$  x = \dfrac{-b \pm \sqrt{b^2 - 4ac}}{2a} $$

\\[ \frac{1}{\Bigl(\sqrt{\phi \sqrt{5}}-\phi\Bigr) e^{\frac25 \pi}} =
1+\frac{e^{-2\pi}} {1+\frac{e^{-4\pi}} {1+\frac{e^{-6\pi}}
{1+\frac{e^{-8\pi}} {1+\ldots} } } } \\]

行内公式： $\Gamma(n) = (n-1)!\quad\forall n\in\mathbb N$
</code></pre>

<p>效果如下（<code>Preferences</code> - <code>Themes</code> - <code>Enable MathJax</code> 才会看到效果）：</p>

<p>块级公式：<br/>
\[  x = \dfrac{-b \pm \sqrt{b^2 - 4ac}}{2a} \]</p>

<p>\[ \frac{1}{\Bigl(\sqrt{\phi \sqrt{5}}-\phi\Bigr) e^{\frac25 \pi}} =<br/>
1+\frac{e^{-2\pi}} {1+\frac{e^{-4\pi}} {1+\frac{e^{-6\pi}}<br/>
{1+\frac{e^{-8\pi}} {1+\ldots} } } } \]</p>

<p>行内公式： \(\Gamma(n) = (n-1)!\quad\forall n\in\mathbb N\)</p>

<h2 id="toc_23">脚注（Footnote）</h2>

<p>Markdown 语法：</p>

<pre><code class="language-text">这是一个脚注：[^sample_footnote]
</code></pre>

<p>效果如下：</p>

<p>这是一个脚注：<sup id="fnref1"><a href="#fn1" rel="footnote">1</a></sup></p>

<h2 id="toc_24">注释和阅读更多</h2>

<!-- comment -->

<!-- more -->

<p>Actions-&gt;Insert Read More Comment <em>或者</em> <code>Command + .</code><br/>
<strong>注</strong> 阅读更多的功能只用在生成网站或博客时，插入时注意要后空一行。</p>

<h2 id="toc_25">TOC</h2>

<p>Markdown 语法：</p>

<pre><code class="language-text">[TOC]
</code></pre>

<p>效果如下：</p>

<ul>
<li>
<a href="#toc_0">Markdown 的设计哲学</a>
</li>
<li>
<a href="#toc_1">本文约定</a>
</li>
<li>
<a href="#toc_2">标题</a>
</li>
</ul>
</li>
<li>
<a href="#toc_3">第一级标题 <code>&lt;h1&gt;</code></a>
<ul>
<li>
<a href="#toc_4">第二级标题 <code>&lt;h2&gt;</code></a>
<ul>
<li>
<ul>
<li>
<ul>
<li>
<ul>
<li>
<a href="#toc_5">第六级标题 <code>&lt;h6&gt;</code></a>
</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
<li>
<a href="#toc_6">强调</a>
</li>
<li>
<a href="#toc_7">换行</a>
</li>
<li>
<a href="#toc_8">列表</a>
<ul>
<li>
<a href="#toc_9">无序列表</a>
</li>
<li>
<a href="#toc_10">有序列表</a>
</li>
<li>
<a href="#toc_11">列表中嵌入代码块语法</a>
</li>
<li>
<a href="#toc_12">任务列表（Task lists）</a>
</li>
</ul>
</li>
<li>
<a href="#toc_13">图片</a>
</li>
<li>
<a href="#toc_14">链接</a>
</li>
<li>
<a href="#toc_15">区块引用</a>
</li>
<li>
<a href="#toc_16">行内代码</a>
</li>
<li>
<a href="#toc_17">多行或者一段代码</a>
</li>
<li>
<a href="#toc_18">顺序图或流程图</a>
</li>
<li>
<a href="#toc_19">表格</a>
</li>
<li>
<a href="#toc_20">删除线</a>
</li>
<li>
<a href="#toc_21">分隔线</a>
</li>
<li>
<a href="#toc_22">MathJax</a>
</li>
<li>
<a href="#toc_23">脚注（Footnote）</a>
</li>
<li>
<a href="#toc_24">注释和阅读更多</a>
</li>
<li>
<a href="#toc_25">TOC</a>
</li>
</ul>


<div class="footnotes">
<hr/>
<ol>

<li id="fn1">
<p>这里是脚注信息&nbsp;<a href="#fnref1" rev="footnote">&#8617;</a></p>
</li>

</ol>
</div>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[deploy mweb to github pages]]></title>
    <link href="https://blog.jajabjbj.top/15812713431650.html"/>
    <updated>2020-02-10T02:02:23+08:00</updated>
    <id>https://blog.jajabjbj.top/15812713431650.html</id>
    <content type="html"><![CDATA[
<pre><code class="language-bash">#!/bin/sh
set -e
# Please set the name and email. 
# Because MWeb can&#39;t get your github global setting.

echo blog.jajabjbj.top &gt; CNAME

gh-pages -d . -b gh-pages -m &quot;{{year}}-{{month}}-{{day}} {{time}}&quot; -r https://${GIT_USER}:${GIT_PASS}@github.com/yunqiangwu/mweb.git

# 这里的 GIT_USER 和 GIT_PASS 要改成自己的

</code></pre>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Hello Work]]></title>
    <link href="https://blog.jajabjbj.top/15266085508343.html"/>
    <updated>2018-05-18T09:55:50+08:00</updated>
    <id>https://blog.jajabjbj.top/15266085508343.html</id>
    <content type="html"><![CDATA[
<pre><code class="language-sequence">Andrew-&gt;China: Says Hello
Note right of China: China thinks about it
China--&gt;Andrew: How are you?
Andrew-&gt;&gt;China: I am good thanks!
</code></pre>

<pre><code class="language-sequence">Andrew-&gt;China: Says Hello
Note right of China: China thinks about it
China--&gt;Andrew: How are you?
Andrew-&gt;&gt;China: I am good thanks!
</code></pre>

<pre><code class="language-flow">st=&gt;start: Start:&gt;http://www.google.com[blank]
e=&gt;end:&gt;http://www.google.com
op1=&gt;operation: My Operation
sub1=&gt;subroutine: My Subroutine
cond=&gt;condition: Yes
or No?:&gt;http://www.google.com
io=&gt;inputoutput: catch something...

st-&gt;op1-&gt;cond
cond(yes)-&gt;io-&gt;e
cond(no)-&gt;sub1(right)-&gt;op1
</code></pre>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[欢迎使用 MWeb]]></title>
    <link href="https://blog.jajabjbj.top/15264405448357.html"/>
    <updated>2018-05-16T11:15:44+08:00</updated>
    <id>https://blog.jajabjbj.top/15264405448357.html</id>
    <content type="html"><![CDATA[
<p>首先介绍一下 MWeb 是什么，MWeb 是专业的 Markdown 写作、记笔记、静态博客生成软件。MWeb 使用的是 Github Flavored Markdown 语法，在使用 MWeb 前，建议你一定要去 <a href="http://zh.mweb.im/">MWeb 官网首页</a> 看一下介绍视频，MWeb 官网也做了比较详细的帮助，建议你也看一下大概内容，帮助的网址为：<a href="http://zh.mweb.im/help.html">http://zh.mweb.im/help.html</a>。</p>

<h2 id="toc_0">需要您重点注意的</h2>

<p>MWeb 是有<strong>两种模式</strong>的：外部模式和文档库模式。MWeb 为了满足所有 Markdown 使用需求，设计了两种模式！外部模式用于打开和编辑所有本地 Markdown 文档。另外为了方便用 Markdown 记笔记，MWeb 设计了文档库模式，文档库中的文档也支持一键生成静态博客，以便于分享。关于两个模式的情况，还是建议您去看一下官网的视频和帮助。</p>

<h2 id="toc_1">帮助我们改进 MWeb</h2>

<p>如果你喜欢 MWeb，想让它变得更好，你可以：</p>

<ol>
<li>推荐 MWeb，让更多的人知道。</li>
<li>给我们发反馈和建议：<a href="mailto:coderforart+233@gmail.com">coderforart+233@gmail.com</a></li>
<li>在 Mac App Store 上评价 （如果是在 MAS 上购买的话）。</li>
</ol>

]]></content>
  </entry>
  
</feed>
